interface:
  display_name: "ReportKit Setup"
  short_description: "Set up automations that send ReportKit Live Activities"
  default_prompt: "Use $reportkit-setup to design a ReportKit Live Activity automation and hand off safe runtime send instructions."
